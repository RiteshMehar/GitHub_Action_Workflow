name: First-Workflow

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install Dependencies
        run: npm install
        
      - name: Building a Project
        run: npm run build

      - name: Setup Node.js
        uses: actions/checkout@v3
        with:
          node-version: 18


    
  
